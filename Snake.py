class Snake:
    def __init__(self, color, length, headX, headY):
        self.color = color
        self.length = length
        self.blocks = []

    # def draw(self, surface):
    #     for block in self.blocks:

    def update(self):
        pass
